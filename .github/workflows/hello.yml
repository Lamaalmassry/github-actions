name: Hello World Example

on:
  push:


jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello
        run: echo "Hello from GitHub Actions 👋"
    

      - name: Print hello adel     
        run: echo "hello adel" 